
fn(main, uint32, args(),
    locals(
        lcl(cond1, bool),
        lcl(cond2, bool),
        lcl(cond3, bool)
    ),
    frame(
        asn(var(cond1, bool),
            litrl(bool, True),
            bool
        )
        if(get(var(cond1, bool), bool),
            frame(
                dropin('c', `puts("True")`)
            )
        )
        else(frame(
                dropin('c', `puts("False")`)
            )
        )
        ret(litrl(uint32, 0), uint32)
    )
)
